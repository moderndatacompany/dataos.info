---
title: "Case Scenario: Table Properties"
---

## List Properties

To obtain the list of properties, execute the following command within the DataOS.

**Command**

```bash
dataos-ctl dataset properties -a ${{udl}}
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-0-1)**Example**

```bash
dataos-ctl dataset properties -a dataos://icebase:retail/city
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-1-1)Output (Successful Execution)

```bash
INFO[0000] 📂 get properties...                          
INFO[0000] 📂 get properties...completed                 

           PROPERTY NAME           | PROPERTY VALUE  
-----------------------------------|-----------------
  write.metadata.compression-codec | gzip
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-2-1)Above, we can see the pre-defined property `write.metadata.compression-codec` whose value is `gzip`

## Add Properties

To add a single property the below code can be used.

**Command**

```bash
dataos-ctl dataset add-properties -a ${{udl}} \
-p "${{property-name}}:${{property-value}}"
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-3-1)To add multiple properties at the same time, use:[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-4-1)

```bash
dataos-ctl dataset add-properties -a dataos://icebase:retail/city \
-p "${{property-name}}:${{property-value}}" \
-p "${{property-name}}:${{property-value}}"
```

**Example**

Let’s say we want to add a new property by the name `write.metadata.metrics.default` and set its value to `full`. To do this, execute the following code:

```bash
dataos-ctl dataset add-properties -a dataos://icebase:retail/city \
-p write.metadata.metrics.default:full
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-5-1)Output (on successful execution)

```bash
INFO[0000] 📂 add properties...                          
INFO[0000] 📂 add properties...completed
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-6-1)To check whether the property is added or not, run the list properties command:

```bash
dataos-ctl dataset properties -a dataos://icebase:retail/city
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-7-1)Output

```bash
INFO[0000] 📂 get properties...                          
INFO[0000] 📂 get properties...completed                 

           PROPERTY NAME           | PROPERTY VALUE  
-----------------------------------|-----------------
  write.metadata.metrics.default   | full            
  write.metadata.compression-codec | gzip
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-8-1)[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-8-2)As we can see, the property has been successfully added.

## Remove Properties

To remove a property, the following command can be used.

**Command**

```bash
dataos-ctl dataset remove-properties -a ${{udl}} \
-p "${{property-name}}" \
-p "${{property-name}}"
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-9-1)**Example**

Let’s say we want to remove the property `write.metadata.metrics.default`. To accomplish this, execute the following code:

```bash
dataos-ctl dataset remove-properties -a dataos://icebase:retail/city \
-p write.metadata.metrics.default
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-10-1)Output (successful execution)

```bash
INFO[0000] 📂 remove properties...                       
INFO[0001] 📂 remove properties...completed
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-11-1)To check whether the property is deleted or not, use the list properties command.

```bash
dataos-ctl dataset properties -a dataos://icebase:retail/city
```

```bash
INFO[0000] 📂 get properties...                          
INFO[0000] 📂 get properties...completed                 

           PROPERTY NAME           | PROPERTY VALUE  
-----------------------------------|-----------------
  write.metadata.compression-codec | gzip
```

[](https://dataos.info/resources/depot/icebase/case_scenario_table_properties/#__codelineno-12-1)As can be observed, the property is successfully deleted.