name: ${{codecommit-r}}
version: ${{v1}}
type: instance-secret
description: ${{"aws codecommit credentials"}}
layer: ${{user}}
instance-secret:
  type: ${{key-value}}
  acl: ${{r}}
  data:
    GITSYNC_USERNAME: ${{"iamgroot"}}
    GITSYNC_PASSWORD: ${{""}}