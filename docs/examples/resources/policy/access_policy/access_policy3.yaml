version: v1
name: test-policy-03
type: policy
layer: user
description: Policy allowing all users
policy:
  access:
    name:  test-policy-03
    description: this is to test policy
    collection: default
    subjects:
      tags:
       - users:id:iamgroot      
    predicates:
      - "**"
    objects:
      tags:
        - dataos:resource:cluster:minerva:system
        - dataos:system:minerva:table:icebase:retail:city
        - dataos:type:secret:icebase_r_r
        - dataos:type:secret:icebase_rw_rw
        - dataos:system:ds:dataset:icebase:retail:city 
    allow: deny          # Granting access