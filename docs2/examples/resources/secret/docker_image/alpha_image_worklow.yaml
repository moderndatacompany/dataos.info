# Alpha
name: example-alpha-workflow
version: v1beta1
type: workflow
workflow:
  dag:
  - name: example
    spec:
      stack: alpha
      alpha:
        image: docker.io/REPOSITORY/IMAGE:TAG
        imagePullSecret: docker-image
